# 포트폴리오 설명 문구 모음

용도별로 길이를 나눠 정리했습니다. 상황에 맞는 것을 골라 쓰시면 됩니다.

---

## 1. 한 줄 소개 (프로필 상단 / 목록용)

> 시리얼·TCP 계측 장비의 데이터를 실시간 수집·저장·모니터링하는 Windows 데스크톱 애플리케이션

---

## 2. 짧은 설명 (3줄, 포트폴리오 카드)

> C#/WinForms 기반 계측 데이터 수집 프로그램입니다.
> 장비에서 오는 데이터를 실시간으로 파싱해 SQLite에 저장하고, 표·차트로 조회하며 임계값 알람과 엑셀 내보내기를 지원합니다.
> 설치 없이 실행 파일 하나로 동작하며, 장비 프로토콜에 맞춰 파서만 교체하면 다른 계측기에도 적용 가능한 구조입니다.

---

## 3. 이력서 항목 (경력기술서 형식)

**계측 데이터 수집·모니터링 프로그램** — 개인 프로젝트
C#, .NET, WinForms, SQLite, TCP Socket

- 시리얼/TCP 계측 장비의 측정 데이터를 실시간 수집·저장·시각화하는 Windows 데스크톱 애플리케이션 개발
- 스트림 경계 처리 및 예외 없는 파싱 구조로 비정상 데이터 수신 시에도 수집 중단 없이 동작, 원문 보존으로 사후 원인 추적 가능하도록 설계
- 수신 스레드와 저장 처리를 분리하고 트랜잭션 단위 일괄 저장을 적용해 디스크 I/O 병목으로 인한 데이터 유실 방지
- 상태 전이 기반 임계값 알람 구현 — 이상 진입·정상 복귀 시점만 기록하여 알람 중복 누적 제거
- 장시간 무인 운전을 전제로 로그·차트·조회 결과에 메모리 상한 적용
- 자체 포함 단일 실행 파일로 배포, 런타임 설치 없이 동작하도록 구성
- 실제 장비 없이 검증 가능하도록 데이터 송신 시뮬레이터를 별도 구현 (불량 데이터 주입 기능 포함)

---

## 4. 제안서용 문단 (위시켓 등)

> 계측 장비 데이터 수집 프로그램을 개발한 경험이 있습니다.
>
> 시리얼·TCP로 들어오는 측정 데이터를 실시간 파싱해 데이터베이스에 저장하고, 표와 차트로 조회하며 임계값을 벗어나면 알람을 기록하는 구조입니다. 엑셀 내보내기까지 포함되어 있습니다.
>
> 이 과정에서 장시간 무인 운전 환경에서 실제로 문제가 되는 부분들 — 깨진 데이터 수신 시 프로그램 중단, 저장 지연으로 인한 데이터 유실, 메모리 누적, 알람 중복 발생 — 을 설계 단계에서 처리했습니다.
>
> 배포는 자체 포함 단일 실행 파일로 구성해, 인터넷이 차단되었거나 소프트웨어 설치 승인이 필요한 현장에서도 파일 하나만 복사하면 동작하도록 했습니다.
>
> 소스 및 상세 설명: https://github.com/chapse57/SerialDataLogger

---

## 5. 면접·미팅에서 말할 것 (2가지만)

길게 말하면 안 듣습니다. 이 둘만 먼저 꺼내고, 나머지는 물어볼 때 답하세요.

> "깨진 데이터가 들어와도 수집이 멈추지 않고, 원본을 남겨서 나중에 원인을 추적할 수 있게 만들었습니다."

> "장시간 켜두는 프로그램이라 무한히 쌓이는 부분 — 로그, 차트, 조회 결과 — 에 전부 상한을 걸었습니다."

---

## 6. 예상 질문과 답변

**Q. 실제 장비 없이 어떻게 검증했나요?**
> 장비 역할을 하는 시뮬레이터를 별도로 만들었습니다. 3개 채널을 한 번에 몰아서 보내도록 해서 스트림이 중간에 잘려 도착하는 상황까지 재현했고, 불량 데이터를 일정 확률로 섞는 옵션도 넣어 파싱 실패 처리를 확인했습니다.

**Q. 실제 시리얼 포트로 바꾸려면 얼마나 걸리나요?**
> 연결부 30줄 정도만 교체하면 됩니다. 파싱·저장·화면은 그대로입니다. 처음부터 계층을 분리해서 설계했습니다.

**Q. 왜 SQLite인가요?**
> PC 한 대에서 동작하는 수집 프로그램이라 별도 DB 서버 설치가 불필요한 SQLite가 적합하다고 판단했습니다. 다만 여러 사용자가 동시에 접근해야 하는 요건이라면 처음부터 다른 선택이 필요합니다.

**Q. 데이터가 계속 쌓이면 어떻게 되나요?**
> 현재는 상한 없이 누적됩니다. 보관 기간 요건이 정해지면 기간 경과분 자동 정리나 월별 파일 분리를 추가하는 방식으로 대응 가능합니다. 조회·차트 쪽은 이미 상한이 걸려 있어 데이터가 늘어도 화면 성능에는 영향이 없습니다.

**Q. 알람을 이메일이나 문자로 보낼 수 있나요?**
> 현재는 화면 표시와 이력 기록까지입니다. 발송 기능은 알람 발생 지점에 연결하면 되는 구조라 추가 가능합니다.

---

## 7. 시연 시나리오 (30초)

GIF나 화면 녹화로 만들 때 순서입니다.

1. 시뮬레이터 **송신 시작** → 수집기 **연결** → 데이터가 실시간으로 흐름
2. 시뮬레이터 **"불량 데이터 섞기"** 체크 → 수집기에 `[오류]` 줄이 섞이지만 **프로그램은 정상 동작**, 오류 카운트만 증가
3. **데이터 표** 탭 → 채널 `(오류)` 선택 → 조회 → 깨진 데이터의 **원문이 그대로 남아 있음**
4. **알람 설정** 탭 → T1 상한 27 입력 → 저장
5. **실시간 로그** 탭 → `[알람] T1 상한 초과` → `[복구] T1 정상 범위로 복귀`, 하단 상태바 색 변화
6. **차트** 탭 → 3채널 실시간 추이
7. **데이터 표** → **엑셀 내보내기** → 엑셀 파일 열림

2번과 3번이 핵심입니다. 다른 지원자와 갈리는 지점입니다.
