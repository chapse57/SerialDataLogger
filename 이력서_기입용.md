# 이력서 기입용

한국식 이력서 양식 칸에 그대로 넣을 수 있게 정리했습니다.

---

## 보유 기술 / 기술 스택 칸

**짧은 버전 (한 줄)**

```
C#, .NET, WinForms, SQLite, SQL, TCP/IP 소켓 통신, Git
```

**보통 버전 (칸이 여유 있을 때)**

```
· 언어         C#, SQL
· 프레임워크   .NET 8/10, WinForms
· 데이터베이스 SQLite
· 통신         TCP/IP 소켓, 시리얼 통신(RS-232)
· 형상관리     Git, GitHub
· 개발도구     Visual Studio
```

**표 형식 (활용 수준까지 쓰는 양식)**

| 구분 | 기술 | 활용 수준 |
|---|---|---|
| 언어 | C# | 데스크톱 응용 프로그램 개발 가능 |
| 언어 | SQL | 테이블 설계 및 조회·집계 쿼리 작성 가능 |
| 프레임워크 | .NET / WinForms | UI 코드 구성 및 이벤트 처리 가능 |
| DB | SQLite | 스키마 설계, 인덱스, 트랜잭션 처리 가능 |
| 통신 | TCP/IP 소켓 | 실시간 데이터 송수신 구현 가능 |
| 기타 | Git | 형상 관리 및 원격 저장소 운용 |

---

## 프로젝트 경험 칸

**양식 A — 기간·역할·내용 구분형**

```
프로젝트명   계측 데이터 수집·모니터링 프로그램
기간         2026.08 (3일)
역할         기획·설계·개발 전담 (1인)
사용 기술    C#, .NET, WinForms, SQLite, TCP/IP 소켓
주요 내용    시리얼·TCP 계측 장비의 측정 데이터를 실시간 수집하여
             데이터베이스에 저장하고, 표·차트 조회와 임계값 알람,
             엑셀 내보내기를 제공하는 Windows 데스크톱 프로그램 개발
성과         설치 없이 실행 파일 하나로 동작하도록 배포 구성
             장비 프로토콜 교체만으로 타 계측기 대응 가능한 구조 설계
```

**양식 B — 줄글형 (칸이 작을 때)**

```
계측 데이터 수집·모니터링 프로그램 개발 (2026.08, 1인 개발)
C#/WinForms 기반으로 시리얼·TCP 장비 데이터를 실시간 수집·저장하고
표·차트 조회, 임계값 알람, 엑셀 내보내기 기능을 구현. SQLite로 데이터를
관리하며 단일 실행 파일로 배포되도록 구성함.
GitHub: github.com/chapse57/SerialDataLogger
```

**양식 C — 불릿형 (자기소개서·경력기술서)**

```
계측 데이터 수집·모니터링 프로그램 (개인 프로젝트, 1인 개발)
사용 기술: C#, .NET, WinForms, SQLite, TCP/IP 소켓

- 시리얼·TCP 장비의 측정 데이터를 실시간 수신·파싱하여 SQLite에 저장
- 기간·채널 조건 조회 화면 및 실시간 추이 차트 구현
- 채널별 임계값 설정 및 알람 발생·복구 이력 관리 기능 구현
- 수집 데이터의 엑셀(xlsx) 내보내기 기능 구현
- 실제 장비 없이 검증 가능하도록 데이터 송신 시뮬레이터 별도 개발
- 자체 포함 단일 실행 파일로 배포하여 런타임 설치 없이 동작하도록 구성
```

---

## 자기소개서에 녹일 때

기술 나열만 하면 안 읽힙니다. 아래처럼 **문제 → 판단 → 결과** 순서로 쓰면 남습니다.

**예시 1 — 데이터 신뢰성**

```
계측 장비의 데이터는 노이즈나 장비 상태에 따라 깨진 형태로 수신되는 경우가
있습니다. 이 경우 프로그램이 중단되면 야간 무인 수집 데이터가 통째로
유실되기 때문에, 파싱 실패를 예외가 아닌 정상 처리 흐름으로 설계하여
잘못된 데이터가 들어와도 수집이 계속되도록 구현했습니다.
또한 실패한 데이터의 원문을 함께 저장하여, 사후에 통신 문제인지 장비
문제인지 구분할 수 있도록 했습니다.
```

**예시 2 — 장시간 운전 고려**

```
24시간 켜두는 프로그램의 특성상 메모리가 계속 증가하는 지점이 문제가
된다고 판단하여, 로그 표시·차트 데이터·조회 결과 각각에 상한을 두었습니다.
개발 단계에서는 드러나지 않지만 장기 운영 시 반드시 발생하는 문제라고
보았기 때문입니다.
```

**예시 3 — 배포 환경 고려**

```
산업 현장의 PC는 인터넷이 차단되어 있거나 소프트웨어 설치에 별도 승인이
필요한 경우가 많습니다. 이를 고려해 .NET 런타임을 포함한 단일 실행 파일로
배포하도록 구성하여, 파일 하나만 복사하면 동작하도록 했습니다.
```

---

## 주의할 점

- **기간을 "3일"로 쓸지 고민된다면** — 개인 프로젝트는 기간을 안 쓰거나 "2026.08"만 써도 됩니다. 3일이라고 쓰면 "그 정도 규모구나"로 읽힐 수 있습니다.
- **"전담"·"1인 개발"은 명시하는 게 낫습니다** — 팀 프로젝트에서 뭘 했는지 불분명한 것보다 낫습니다.
- **못 한 건 쓰지 마세요** — 히스테리시스, 다중 장비 동시 수집, 실제 시리얼 포트 연동은 아직 안 했습니다. 물어보면 "구조는 잡아뒀고 요건이 확정되면 추가하는 방식"이라고 답하면 됩니다.
- **"SQL 사용"보다 "테이블 설계"가 낫습니다** — 실제로 스키마를 직접 설계하고 인덱스를 걸었으니 그렇게 쓰는 게 정확합니다.
